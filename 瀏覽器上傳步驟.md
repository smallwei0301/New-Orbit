# 用瀏覽器完成（不需要終端機）

## 步驟 1：把檔案上傳到 GitHub

1. 解壓縮 `New-Orbit-upload.zip`，會得到一個 `upload-New-Orbit` 資料夾。
2. 打開 <https://github.com/smallwei0301/New-Orbit>
3. 點頁面上的 **uploading an existing file**（或 **Add file → Upload files**）。
4. 把 `upload-New-Orbit` 資料夾**裡面的所有東西**（不是資料夾本身）一起拖進去。
   - 直接把資料夾內容全選拖曳即可，GitHub 會保留 `src/`、`server/`、`api/` 這些子目錄結構。
5. 最下方 Commit 訊息隨意，按 **Commit changes**。

> 上傳內容不含任何金鑰：`.env` 已排除，我也掃過確認沒有 service_role 金鑰。

## 步驟 2：Vercel 匯入

1. 打開 <https://vercel.com/new>
2. 選 **Import Git Repository** → 找到 `New-Orbit` → **Import**
3. Framework 會自動偵測成 **Vite**，build 設定由 `vercel.json` 決定，**不用改**。
4. **先不要按 Deploy**。展開 **Environment Variables**，加入這 3 個：

| Name | Value |
|---|---|
| `SUPABASE_URL` | `https://ourhzenuvampgmdkysrn.supabase.co` |
| `SUPABASE_SERVICE_ROLE_KEY` | 到 Supabase → Project Settings → API → service_role → Reveal 複製 |
| `AUTH_SECRET` | `ai00tXzudDYqOWBsbUjgtUAbkFMihv71hOBWxlnJFd8tP03edaBtndXQVq6s-jWM` |

> 三個都**不要**加 `VITE_` 前綴。加了會被打包進前端，等於把金鑰公開。

5. 按 **Deploy**，等 1～2 分鐘。

## 步驟 3：確認有沒有成功

部署完成後，把網址貼給我，我可以直接幫你驗證。你自己也可以先看兩件事：

1. 打開 `https://<你的網域>/midao/dashboard`
   → 應該看到登入頁，用 `demo@orbit.test` / `demo1234` 登入後看到「早安，商家」與示範資料。

2. 在瀏覽器網址列打開：
   `https://<你的網域>/api/v1/customers?orgId=midao`
   → **應該看到 `{"code":"INVALID_TOKEN","message":"請重新登入"}`**
   這代表 API 有正確擋下未授權存取。
   若看到客戶資料，代表驗證沒生效，請告訴我。
   若看到 `INTERNAL` 或 500，代表環境變數沒設好，回 Vercel 檢查後 **Redeploy**。

## 常見狀況

- **Vercel 找不到 New-Orbit**：Vercel 第一次要授權存取你的 GitHub。在 Import 頁面點 **Adjust GitHub App Permissions**，把 `New-Orbit` 加進允許清單。
- **改了環境變數但沒生效**：Vercel 的環境變數要重新部署才會套用。到 Deployments → 最新那筆右側 **⋯ → Redeploy**。
